<!-- iterate through array of possible custom component, only render the one is list in UI metadata -->
{{#each this.uiConfigs as |uiConfig|}}
  {{#if uiConfig.shouldRender}}
    {{component uiConfig.uiType uiConfigs=uiConfig}}
  {{/if}}
{{/each}}